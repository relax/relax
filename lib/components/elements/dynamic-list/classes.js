import jss from '../../../helpers/stylesheet';

export default jss.createRules({
  row: {
    display: 'block',
    textAlign: 'left'
  },
  column: {
    display: 'table-cell',
    verticalAlign: 'top',
    position: 'relative'
  }
});
