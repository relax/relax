import jss from '../../../helpers/stylesheet';

export default jss.createRules({
  row: {
    display: 'block',
    textAlign: 'left'
  },
  column: {
    display: 'inline-block',
    verticalAlign: 'top',
    position: 'relative'
  }
});
